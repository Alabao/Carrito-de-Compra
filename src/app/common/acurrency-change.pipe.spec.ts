import { AcurrencyChangePipe } from './acurrency-change.pipe';

describe('AcurrencyChangePipe', () => {
  it('create an instance', () => {
    const pipe = new AcurrencyChangePipe();
    expect(pipe).toBeTruthy();
  });
});
